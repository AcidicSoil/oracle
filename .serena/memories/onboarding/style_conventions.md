Use existing TypeScript ESM style with named exports, small helpers, and targeted normalization/validation logic. Preserve current formatting as enforced by oxfmt. Use oxlint/typecheck-clean code, keep changes surgical, and prefer modifying shared helpers when multiple config paths depend on the same behavior. Tests are written with Vitest using focused `describe/test/expect` cases.